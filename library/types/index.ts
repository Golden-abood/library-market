export type Category = {
  text: string;
  icon: string;
};
export type HeadText = {
  text: string;
  title: string;
};

export type Author = {
  avatar: string;
  discover: string;
  title: string;
  current: string;
  end: string;
};
export type ItemsMarket = {
  id: number;
  photo: string;
  title: string;
  current: string;
  sa: string;
  end: string;
  date: string;
  category: string;
  username: string;
  email: string;
  avatar: string;
};
